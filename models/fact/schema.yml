
version: 2

models:
  - name: fact_boarding_passes
    description: "aircraft dimension model"
    columns:
      - name: aircraft_code
        description: "The primary key for this table"
        data_tests:
          - not_null


  - name: fact_flights
    description: "airport dimension model"
    columns:
      - name: airport_code
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null